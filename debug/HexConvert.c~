#include <stdio.h>
char one, two, three, four;

int main(int argc, char *argv[])
{
    char c;
    while(getchar() != 0x22);
    while((one = getchar()) != EOF){
        two = getchar();
        three = getchar();
        four = getchar();
        printf("\n%2x!%2x!%2x!%2x! : ", four, three, two , one);
        getchar();
        getchar();
        getchar();
        getchar();
    }
    return 0;
}
